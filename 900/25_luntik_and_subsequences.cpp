#include <bits/stdc++.h>
#include <iostream>
using namespace std;

int main()
{
#ifndef ONLINE_JUDGE
    freopen("../input.txt", "r", stdin);
    freopen("../output.txt", "w", stdout);
#endif

    int t;
    cin >> t;
    while (t--)
    {
        long long n, m, i, j;
        cin >> n >> m >> i >> j;
        cout << "1 1 " << n << " " << m << endl;
    }

    // cout << __gcd(0, 9);

    return 0;
}